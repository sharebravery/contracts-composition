# Contract Registry

部署地址和外部协议地址的唯一来源。

Phase 1 合约已经部署到 Arbitrum Sepolia，真实地址、部署区块和交易哈希记录在 `deployments/421614.json`。

四条主合约记录均已完成 Arbiscan 源码验证并标记为 `verified: true`。Web 应用会读取这些 verified Registry 记录并启用对应的链上读操作。
